# 2단계: 시스템 설계서 (TactiCore)

> 선행 문서: `1단계_요구사항_정의.md` — 본 문서의 각 설계 항목은 해당 문서의 요구사항 ID(FR-01~FR-08)를 참조한다.

## 0. 이 단계에서 확정한 결정

| 항목 | 결정 | 근거 |
| --- | --- | --- |
| DB 저장 구조 | **정규화 테이블 방식** (`analyses` / `players` / `phases` / `positions`) | 요구사항 §5.2의 기본안. 목록 조회 시 좌표를 읽지 않아도 되고, 2차의 타임라인 확장에서 국면 행만 늘리면 됨 |
| 축소 대안 | `analyses.payload_json` 단일 컬럼 저장 | 일정 압박 시의 탈출구. 채택 시 3·4단계 문서의 DB/모델 절만 교체하면 되도록 격리해 설계 |
| 피치 방향 | 세로(포트레이트), 공격 방향 = y 감소 | 요구사항의 "5채널 그리드(세로 5분할)"·`pressingLineY` 필드와 정합 |
| 컨테이너화 | 사용하지 않음 | MVP는 로컬 2프로세스 실행. Docker는 공개 배포를 결정하는 2차에서 재검토 |

## 1. 전체 아키텍처

MVP는 공개 배포 없이 PC 한 대에서 두 개의 프로세스로 동작한다.

```
┌──────────────────────────── 사용자 PC ────────────────────────────┐
│                                                                   │
│  ┌─────────────────────────┐        ┌──────────────────────────┐  │
│  │  Browser (Chrome)       │        │  FastAPI (uvicorn)       │  │
│  │  http://localhost:5173  │        │  http://localhost:8000   │  │
│  │                         │        │                          │  │
│  │  React 18 + TS (Vite)   │        │  routers/analyses.py     │  │
│  │   ├ Pitch (SVG)         │  REST  │        │                 │  │
│  │   ├ SpaceLayers         │ ─────► │  crud.py                 │  │
│  │   ├ Editor Panel        │  JSON  │        │                 │  │
│  │   ├ ShareCard (export)  │ ◄───── │  models.py (SQLAlchemy)  │  │
│  │   └ AnalysisList        │        │        │                 │  │
│  │                         │        │        ▼                 │  │
│  │  Zustand (편집 상태)     │        │  ┌──────────────────┐    │  │
│  │  React Query (서버 상태) │        │  │ SQLite           │    │  │
│  └───────────┬─────────────┘        │  │ tacticore.db     │    │  │
│              │                      │  └──────────────────┘    │  │
│              ▼ (브라우저 다운로드)     └──────────────────────────┘  │
│      PNG 카드 / .json 파일                                         │
└───────────────────────────────────────────────────────────────────┘
```

**핵심 설계 원칙 — 백엔드는 선택적 의존성이다 (FR-08).**
편집·시각화·PNG 내보내기·JSON 입출력은 전부 브라우저 안에서 완결된다. 백엔드는 "저장·목록"만 담당하므로, 서버가 꺼져 있어도 앱의 주 기능은 동작해야 한다. 프론트는 시작 시 `GET /api/health`로 서버 가용 여부를 확인하고, 실패하면 저장/목록 UI만 비활성화한다.

## 2. 프로젝트 디렉토리 구조

```
soccer/
├── CLAUDE.md
├── docs/                          # 1~6단계 문서
├── backend/
│   ├── main.py                    # FastAPI 앱, CORS, 라우터 등록
│   ├── database.py                # 엔진/세션, Base
│   ├── models.py                  # SQLAlchemy ORM (5개 테이블 — annotations 포함)
│   ├── schemas.py                 # Pydantic 스키마 (요청/응답)
│   ├── crud.py                    # DB ↔ 분석 JSON 변환 로직
│   ├── routers/
│   │   └── analyses.py            # /api/analyses 엔드포인트
│   ├── requirements.txt
│   └── tacticore.db               # (gitignore)
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── pitch/             # Pitch, PlayerNode, ChannelGrid,
│   │   │   │                      # PressingLine, CompactnessBox, OverloadLayer
│   │   │   ├── editor/            # PhaseTabs, FormationPicker, PlayerForm, CommentPanel
│   │   │   ├── export/            # ShareCard (고정 비율 캡처 노드)
│   │   │   └── analyses/          # AnalysisList, SaveDialog
│   │   ├── hooks/                 # useAnalyses, useAnalysisMutation, useServerHealth
│   │   ├── lib/
│   │   │   ├── api.ts             # fetch 래퍼, snake_case ↔ camelCase 변환
│   │   │   ├── coords.ts          # 좌표계 변환 (백분율 ↔ SVG)
│   │   │   ├── zones.ts           # 5채널 / 수직 3분할 경계 정의
│   │   │   ├── overload.ts        # 오버로드 계산
│   │   │   ├── compactness.ts     # 콤팩트니스/압박 라인 산출
│   │   │   ├── formations.ts      # 포메이션 프리셋 좌표
│   │   │   ├── schema.ts          # JSON 검증 (zod)
│   │   │   └── exportImage.ts     # html-to-image 캡처
│   │   ├── store/analysisStore.ts # Zustand 편집 상태
│   │   ├── types/analysis.ts      # 공용 타입
│   │   └── App.tsx
│   ├── public/samples/            # 샘플 경기 JSON
│   └── package.json
└── scripts/
    └── dev.ps1                    # 프론트+백엔드 동시 기동 (요구사항 §4.5)
```

## 3. 좌표계 및 피치 설계 (FR-01)

모든 좌표는 픽셀이 아닌 **0~100 백분율**이다. 피치 크기·화면 크기와 무관하게 동일한 데이터가 동작한다.

```
        x=0                                   x=100
         ┌─────────────────────────────────────┐  y=0   ← 상대 골문 (공격 방향)
         │             상대 진영                 │
         │ ─────────── 하프라인 ─────────────── │  y=50
         │              자 진영                  │
         └─────────────────────────────────────┘  y=100 ← 자팀 골문
```

| 축 | 0 | 100 | 비고 |
| --- | --- | --- | --- |
| `x` | 왼쪽 터치라인 | 오른쪽 터치라인 | 5채널은 x축을 분할 |
| `y` | 상대 골라인 | 자팀 골라인 | 값이 **작을수록 전진**. `pressingLineY`도 이 축 |

- 실측 환산 기준: 피치 105m × 68m. `y` 1% ≈ 1.05m, `x` 1% ≈ 0.68m. 콤팩트니스 수치 표기에 사용한다.
- **좌표는 항상 분석 대상 팀 기준으로 정규화한다.** 분석 대상 팀이 홈이든 원정이든, 그 팀은 언제나 `y=100`(자기 골문)에서 `y=0`(상대 골문) 방향으로 공격한다. 따라서 `match.analyzedTeam`(`home`/`away`)은 **어느 팀을 분석했는지 표시하기 위한 메타데이터일 뿐, 좌표 해석이나 피치 방향을 바꾸지 않는다.** 이 규칙이 없으면 압박 라인(최대 y)·공격 서드(y 0~33.3) 같은 계산이 분석 대상에 따라 뒤집혀야 하는데, 그렇게 설계하지 않는다.
- 렌더링은 SVG `viewBox="0 0 100 100"` + `preserveAspectRatio`로 처리해, 좌표를 변환 없이 그대로 사용한다. Canvas 대신 SVG를 쓰는 이유는 선수 노드가 개별 DOM 요소여야 Framer Motion 모핑(§8)과 드래그를 붙일 수 있기 때문이다.

### 3.1 채널 경계 (FR-03)

5채널은 균등 20% 분할이 아니라 **페널티 지역·골 지역 폭을 연장한 실제 기준선**으로 나눈다.

| 채널 | x 범위 | 기준 |
| --- | --- | --- |
| 좌측 측면 | 0 ~ 20 | 페널티 지역 좌측 라인 연장 |
| 좌측 하프스페이스 | 20 ~ 36.5 | 페널티 지역 ~ 골 지역 사이 |
| 중앙 | 36.5 ~ 63.5 | 골 지역 폭 연장 |
| 우측 하프스페이스 | 63.5 ~ 80 | |
| 우측 측면 | 80 ~ 100 | |

수직 방향은 오버로드 계산을 위해 3분할한다: 공격 서드 `y 0~33.3`, 중원 `33.3~66.7`, 수비 서드 `66.7~100`. 채널 5 × 서드 3 = **15개 구역**이 오버로드 판정 단위다.

## 4. 데이터베이스 설계 (SQLite)

```sql
CREATE TABLE analyses (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    match_name      TEXT    NOT NULL,
    home_team       TEXT    NOT NULL,
    away_team       TEXT    NOT NULL,
    match_date      TEXT    NOT NULL,             -- YYYY-MM-DD
    competition     TEXT,
    analyzed_team   TEXT    NOT NULL,             -- 'home' | 'away'
    formation       TEXT    NOT NULL,             -- '4-3-3'
    summary         TEXT,
    schema_version  INTEGER NOT NULL DEFAULT 1,
    created_at      TEXT    NOT NULL,
    updated_at      TEXT    NOT NULL
);

CREATE TABLE players (
    id           INTEGER PRIMARY KEY AUTOINCREMENT,
    analysis_id  INTEGER NOT NULL REFERENCES analyses(id) ON DELETE CASCADE,
    client_id    TEXT    NOT NULL,                -- JSON의 players[].id
    name         TEXT    NOT NULL,
    number       INTEGER NOT NULL,
    role         TEXT,
    UNIQUE (analysis_id, client_id)
);

CREATE TABLE phases (
    id               INTEGER PRIMARY KEY AUTOINCREMENT,
    analysis_id      INTEGER NOT NULL REFERENCES analyses(id) ON DELETE CASCADE,
    phase_type       TEXT    NOT NULL,            -- 'base' | 'attack' | 'defense'
    pressing_line_y  REAL,                        -- NULL이면 자동 산출
    comment          TEXT,
    UNIQUE (analysis_id, phase_type)
);

CREATE TABLE positions (
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    phase_id   INTEGER NOT NULL REFERENCES phases(id) ON DELETE CASCADE,
    player_id  INTEGER REFERENCES players(id) ON DELETE CASCADE,  -- 상대팀이면 NULL
    side       TEXT    NOT NULL,                  -- 'own' | 'opponent'
    slot       INTEGER NOT NULL,                  -- 상대팀 좌표의 순서 보존용 (0~10)
    x          REAL    NOT NULL,
    y          REAL    NOT NULL
);

CREATE TABLE annotations (                        -- 전술 그리기 화살표 (2026-09-01 추가)
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    phase_id   INTEGER NOT NULL REFERENCES phases(id) ON DELETE CASCADE,
    client_id  TEXT    NOT NULL,                  -- JSON의 annotations[].id
    ann_type   TEXT    NOT NULL,                  -- 'run'(실선) | 'pass'(점선)
    from_x     REAL    NOT NULL,
    from_y     REAL    NOT NULL,
    to_x       REAL    NOT NULL,
    to_y       REAL    NOT NULL
);

CREATE INDEX idx_positions_phase ON positions(phase_id);
CREATE INDEX idx_annotations_phase ON annotations(phase_id);
CREATE INDEX idx_analyses_updated ON analyses(updated_at DESC);
```

설계 메모:
- **`client_id`를 별도로 둔다.** JSON 안의 선수 id(문자열)는 프론트가 만들고, DB의 정수 PK와 별개다. 저장·재로드를 반복해도 JSON 쪽 id가 안 바뀌어야 모핑 애니메이션의 노드 동일성(§8)이 유지된다.
- **`side='opponent'`인 행은 `player_id`가 NULL이다.** 상대팀은 이름·등번호를 입력받지 않으므로(요구사항 FR-06) 좌표만 저장하며, `slot`으로 순서를 보존한다.
- **화살표는 phases에 컬럼을 얹지 않고 별도 `annotations` 테이블에 담는다.** `Base.metadata.create_all`은 새 테이블은 자동 생성하지만 기존 테이블에 컬럼을 추가하지는 않으므로, 별도 테이블이면 기존 DB(`tacticore.db`)에서 마이그레이션 없이 동작한다. 끝점은 자유 좌표(선수 부착 아님)라 `player_id`가 없다.
- SQLite는 기본적으로 FK를 강제하지 않는다. 커넥션마다 `PRAGMA foreign_keys=ON`을 실행해야 `ON DELETE CASCADE`가 동작한다(3단계에서 이벤트 리스너로 지정).
- 요구사항 §4.4의 "상대팀은 입력 시 11명 전원" 제약은 DB 제약으로 표현이 어렵다. **Pydantic(서버)과 zod(클라이언트) 양쪽 검증으로 강제**한다.

## 5. API 설계 개요 (FR-08)

기본 규칙:
- 베이스 경로 `/api`, 요청·응답 본문은 JSON.
- **서버는 snake_case, 프론트는 camelCase**를 쓰고, `lib/api.ts`에서 자동 변환한다.
- 분석 상세(`GET /{id}`)의 응답 본문은 요구사항 §5.1의 JSON 스키마와 **완전히 동일한 형태**다. 그래야 JSON 파일 가져오기(FR-07)와 서버 조회가 같은 파서를 공유한다.
- 오류는 `{"detail": "..."}` 형태(FastAPI 기본)로 반환한다.

| 메서드 | 경로 | 요청 | 응답 | FR |
| --- | --- | --- | --- | --- |
| `GET` | `/api/health` | — | `{"status":"ok"}` | FR-08 (가용성 확인) |
| `GET` | `/api/analyses` | — | 목록 요약 배열 | FR-08 |
| `GET` | `/api/analyses/{id}` | — | 분석 전체 JSON | FR-08 |
| `POST` | `/api/analyses` | 분석 전체 JSON | 생성된 분석(+id) | FR-08 |
| `PUT` | `/api/analyses/{id}` | 분석 전체 JSON | 갱신된 분석 | FR-08 |
| `DELETE` | `/api/analyses/{id}` | — | `204 No Content` | FR-08 |

목록 응답에는 좌표를 싣지 않는다(`id`, `matchName`, `homeTeam`, `awayTeam`, `matchDate`, `competition`, `updatedAt`만). 상세 규격은 3단계 문서에서 확정한다.

**갱신은 전체 교체(replace) 방식이다.** `PUT` 시 하위 `players`/`phases`/`positions`를 지우고 다시 삽입한다. 부분 갱신(PATCH)은 MVP에서 지원하지 않는다 — 편집 화면이 항상 분석 전체를 들고 있으므로 부분 갱신의 이득이 없다.

## 6. 프론트엔드 라우팅

| 경로 | 화면 | 비고 |
| --- | --- | --- |
| `/` | 편집기 (피치 + 분석 패널) | 진입 시 빈 상태 또는 마지막 편집본 |
| `/new` | 포메이션 프리셋 선택 → 편집기 진입 | FR-06 |
| `/analyses` | 저장된 분석 목록 | FR-08. 서버 미기동 시 안내 화면 |
| `/analyses/:id` | 해당 분석을 불러온 편집기 | FR-08 |

라우터는 `react-router-dom`을 사용한다. 화면이 4개뿐이므로 코드 스플리팅은 하지 않는다.

## 7. 상태 관리 전략

두 종류의 상태를 명확히 분리한다.

| 구분 | 도구 | 대상 |
| --- | --- | --- |
| 서버 상태 | React Query | 분석 목록, 분석 상세 조회, 저장/삭제 뮤테이션, 서버 헬스 |
| 편집 상태 | Zustand (`analysisStore`) | 현재 편집 중인 분석 전체(경기정보·선수·국면별 좌표·코멘트), 선택된 국면, 레이어 토글 |

- 편집 상태를 React Query 캐시에 두지 않는다. 드래그 중 좌표가 초당 수십 회 바뀌므로, 서버 캐시와 섞이면 불필요한 무효화·재요청이 생긴다.
- 레이어 토글(5채널/하프스페이스/압박라인/콤팩트니스/오버로드)은 **분석 데이터가 아니라 화면 설정**이다. 저장 대상에서 제외하고 스토어의 별도 슬라이스에 둔다.
- 저장(FR-08)은 스토어의 현재 스냅샷을 §5.1 JSON으로 직렬화해 뮤테이션에 넘긴다. 직렬화 함수는 JSON 내보내기(FR-07)와 **동일한 함수를 공유**한다.

## 8. 국면 모핑 설계 (FR-02)

- 선수 노드는 Framer Motion의 `motion.g`로 렌더링하고, **React key는 반드시 `playerId`를 쓴다.** 배열 인덱스를 key로 쓰면 국면 전환 시 노드 동일성이 깨져 엉뚱한 선수끼리 위치를 교환하는 애니메이션이 나온다. 이 프로젝트에서 가장 만들기 쉬운 버그다.
- 선수 노드는 `<motion.g>`로 감싸고 **`animate={{ x, y }}`(transform)로 보간**한다. `circle`의 `cx`/`cy` 속성을 직접 애니메이션하지 않는다 — 속성 애니메이션은 매 프레임 레이아웃을 다시 계산시키고, 자식 요소(등번호 텍스트)가 함께 움직이지 않는다. `layout` 애니메이션은 SVG에서 신뢰도가 낮으므로 사용하지 않는다.
- 전환 사양: `duration 0.6s`, `ease: [0.4, 0, 0.2, 1]`. 국면 전환 중 다른 국면 버튼을 누르면 현재 위치에서 새 목표로 이어서 보간한다(Framer Motion 기본 동작).
- **Ghost View**: 전환 직전 국면의 좌표를 `opacity 0.25`의 정지 노드로 함께 렌더링하고, 이전 위치 → 현재 위치를 잇는 점선 경로를 그린다. 전환이 끝나고 2초 후 페이드아웃하거나, 토글로 고정할 수 있다.
- 애니메이션 진행 여부를 스토어의 `isMorphing` 플래그로 노출한다. PNG 내보내기(§10)가 이 값을 보고 대기한다.

## 9. 공간 시각화 레이어 설계 (FR-03)

레이어는 각각 독립 토글이며, 피치 SVG 위에 순서대로 겹친다: 채널 그리드 → 콤팩트니스 → 압박 라인 → 오버로드 → 선수 노드(항상 최상단).

| 레이어 | 산출 방식 | 데이터 요구 |
| --- | --- | --- |
| 5채널 그리드 | §3.1 고정 경계선 렌더링 | 없음(정적) |
| 하프스페이스 강조 | 채널 2·4 영역을 반투명 채색 | 없음(정적) |
| 압박 라인 | `pressingLineY` 값에 수평선. NULL이면 **GK를 제외한 자팀 선수 중 최대 y**로 자동 산출 | 자팀 좌표 |
| 콤팩트니스 | 자팀(GK 제외) 좌표의 bounding box를 그리고, 세로·가로 길이를 m로 환산 표기 | 자팀 좌표 |
| 오버로드 | §9.1 | **상대팀 좌표 필수** |

GK 판별은 `role` 문자열이 아니라 **y값이 가장 큰 선수 1명**으로 한다. 역할 문자열은 자유 입력이라 신뢰할 수 없다.

### 9.1 오버로드 계산

```
입력: 국면 phase (자팀 11 좌표, 상대팀 11 좌표)
출력: 15개 구역별 { own, opp, diff, level }

for each zone in (5채널 × 3서드):
    own = zone 안에 포함된 자팀 선수 수
    opp = zone 안에 포함된 상대팀 선수 수
    diff = own - opp
    level = diff >= 2 ? 'strong'
          : diff == 1 ? 'weak'
          : 'none'
```

- 경계선 위의 좌표는 **작은 쪽 구역에 포함**한다(하한 포함, 상한 배제). 즉 `x ∈ [20, 36.5)`이면 좌측 하프스페이스.
- `strong`은 진한 채색, `weak`는 옅은 채색, `none`은 렌더링하지 않는다. 수적 열세(diff < 0)는 MVP에서 표시하지 않는다.
- 상대팀 좌표가 없으면 이 레이어의 토글 자체를 비활성(disabled)으로 만들고, 툴팁으로 "상대팀 위치를 입력하면 사용할 수 있습니다"를 안내한다(요구사항 FR-03).

## 10. 이미지 내보내기 설계 (FR-05)

- 캡처 대상은 화면의 반응형 레이아웃이 **아니라**, 화면 밖에 고정 크기로 렌더링해 둔 `ShareCard` 노드다. 뷰포트에 따라 결과물이 달라지는 것을 막는다.
- 카드 규격: 1:1 = 1080×1080, 4:5 = 1080×1350 (px). 사용자가 비율을 고른다.
- 카드 구성: 상단 경기 정보 → 피치(현재 국면 + 켜져 있는 레이어) → 하단 텍스트.
- **하단 텍스트는 "현재 국면 코멘트"와 "종합 평가(`summary`)" 중 선택한다.** 국면별로 카드를 3장 뽑고 마지막에 종합 평가 카드 1장을 더 뽑는 것이 카드 뉴스의 일반적인 사용 흐름이므로, 종합 평가가 이미지로 나갈 경로가 반드시 있어야 한다. 이 선택지가 없으면 사용자가 입력한 `summary`는 앱과 JSON 안에만 남고 결과물에는 전혀 등장하지 않는다.
- 캡처 절차:
  1. `isMorphing`이 `true`면 완료까지 대기한다(§8).
  2. 웹폰트 로드 완료(`document.fonts.ready`)를 기다린다. 이걸 빠뜨리면 글자가 폴백 폰트로 찍힌다.
  3. `html-to-image`의 `toPng`를 `pixelRatio: 2`로 호출한다.
  4. 생성된 data URL을 `<a download>`로 저장한다.
- 화면 밖 배치는 `position: absolute; left: -9999px`를 쓴다. `display: none`이나 `visibility: hidden`은 html-to-image가 캡처하지 못한다.

## 11. 화면 레이아웃 (FR-01, FR-03, FR-04)

배치 원칙은 하나다. **피치를 보면서 조작하는 것은 피치 옆에, 타이핑하는 것은 패널에** 둔다. 국면 전환과 레이어 토글은 결과가 피치에 즉시 나타나므로 시선이 이동하면 안 되고, 텍스트 입력은 피치를 안 봐도 되므로 패널로 뺀다.

### 11.1 데스크톱 (≥ 1024px)

```
┌────────────────────────────────────────────────────────────────┐
│ TactiCore    맨시티 vs 아스날 · 2026-08-30    [저장] [PNG] [⋯] │  헤더 56px
├──────────────────────────────────┬─────────────────────────────┤
│  ┌────────────────────────────┐  │  경기 정보                   │
│  │ [기본] [공격] [수비]  ⟳Ghost│  │  ┌───────────────────────┐  │
│  └────────────────────────────┘  │  │ 매치명 / 홈 / 원정      │  │
│                                  │  │ 일자 / 대회 / 분석 대상  │  │
│      ┌──────────────────┐        │  └───────────────────────┘  │
│      │                  │        │                             │
│      │      피치        │        │  선수 (11)                   │
│      │   68 : 105 비율   │        │  ┌───────────────────────┐  │
│      │                  │        │  │ 31 에데르송      GK    │  │
│      │                  │        │  │ 2  워커         RB    │  │
│      └──────────────────┘        │  │ …                     │  │
│                                  │  └───────────────────────┘  │
│  [5채널][하프][압박][콤팩][오버]  │                             │
│   ← 레이어 토글 칩                │  공격 국면 코멘트            │
│                                  │  ┌───────────────────────┐  │
│                                  │  │ (textarea)            │  │
│                                  │  └───────────────────────┘  │
│                                  │  종합 평가                   │
│                                  │  ┌───────────────────────┐  │
│                                  │  │ (textarea)            │  │
│                                  │  └───────────────────────┘  │
└──────────────────────────────────┴─────────────────────────────┘
        좌: flex-1 (최소 480px)        우: 400px 고정 (min 360 / max 480)
```

- **국면 탭은 피치 바로 위**, **레이어 토글 칩은 피치 바로 아래**에 둔다. 둘 다 피치와 시선 거리가 최소여야 한다.
- Ghost View 토글은 국면 탭 우측에 붙인다 — 국면 전환과 한 묶음의 기능이기 때문이다.
- 코멘트 입력창의 제목은 현재 국면명을 따라 바뀐다(`공격 국면 코멘트`). 어느 국면에 쓰는 중인지 헷갈리는 것이 이 앱에서 가장 흔한 실수다.
- 우측 패널은 세로 스크롤한다. 피치는 스크롤되지 않고 고정된다.

### 11.2 모바일 (< 768px)

세로 1단 스택. 위에서부터:

1. 헤더 (앱명 + 경기명 축약)
2. **국면 탭 — `sticky top-0`** 으로 상단에 고정. 스크롤해도 국면 전환은 항상 가능해야 한다.
3. 피치 (가로 100%, 비율 유지)
4. 레이어 토글 칩 — 가로 스크롤(`overflow-x-auto`). 5개를 줄바꿈하지 않는다.
5. 현재 국면 코멘트
6. 선수 목록 (접기/펼치기, 기본 접힘)
7. 종합 평가
8. **하단 고정 액션 바** — `[저장] [PNG 내보내기]`. `sticky bottom-0`.

태블릿(768~1023px)은 모바일 레이아웃을 쓰되 좌우 여백만 넓힌다. 2단 구성은 1024px부터 적용한다.

### 11.3 화면별 구성

| 경로 | 구성 |
| --- | --- |
| `/` | 위 편집기 레이아웃. 분석이 없으면 빈 피치 + "새 분석 시작" 안내 |
| `/new` | 포메이션 프리셋 카드 4장(4-3-3 / 4-4-2 / 4-2-3-1 / 3-5-2) 선택 → `/`로 이동 |
| `/analyses` | 저장 목록 테이블(매치명·팀·일자·수정일시·삭제). 서버 미기동 시 목록 대신 안내 카드 |
| `/analyses/:id` | `/`와 동일 레이아웃, 해당 분석 로드 |

### 11.4 피치 렌더링 규격

- 종횡비 `68 : 105`(가로:세로). CSS `aspect-ratio: 68 / 105`로 고정한다.
- 데스크톱에서 최대 높이는 뷰포트 높이 - 헤더 - 탭 - 토글 칩. 피치가 잘리면 안 된다.
- SVG는 `viewBox="0 0 100 100"` + `preserveAspectRatio="xMidYMid meet"`(§3).

## 12. 색 및 시각 규정

**이 프로젝트에서 색은 장식이 아니라 데이터다.** 오버로드 강도(2단계), 팀 구분(자팀/상대팀), 레이어 종류가 전부 색으로만 구별된다. 구현 중 즉흥적으로 정하면 의미가 충돌한다.

### 12.1 피치

| 요소 | 값 | 비고 |
| --- | --- | --- |
| 피치 배경 | `#1B5E3F` (deep green) | 진한 초록. SNS 카드에서 대비가 확보되고, 위에 얹는 반투명 레이어가 전부 잘 보인다 |
| 피치 라인 | `#FFFFFF` opacity `0.55`, 두께 `0.3` (SVG 단위) | |
| 하프라인·센터서클 | 동일 | |

앱 UI 전체는 라이트 모드(흰 배경)지만 **피치만 진한 초록**이다. 흰 피치 위에 반투명 채색을 얹으면 오버로드 `weak`(15% 농도)가 배경과 구분되지 않는다.

### 12.2 선수 노드

| 대상 | 채움 | 테두리 | 텍스트 | 반지름 |
| --- | --- | --- | --- | --- |
| 자팀 | `#F8FAFC` | `#0F172A` 0.4 | `#0F172A` | `2.6` |
| 상대팀 | `#94A3B8` opacity `0.55` | 없음 | 없음 | `2.2` |
| Ghost (이전 국면) | 자팀 색 opacity `0.25` | 없음 | 없음 | `2.6` |

- 상대팀은 **의도적으로 시각적 무게를 낮춘다.** 분석 대상은 자팀이고, 상대팀은 오버로드 판정을 위한 참조 정보다. 같은 굵기로 그리면 어느 팀 이야기인지 읽히지 않는다.
- 상대팀에는 이름·등번호가 없으므로(FR-06) 텍스트를 그리지 않는다.
- Ghost 이동 경로 점선: `#F8FAFC` opacity `0.35`, `stroke-dasharray="1 1.5"`.

### 12.3 공간 레이어

| 레이어 | 색 | 형태 |
| --- | --- | --- |
| 5채널 그리드 | `#FFFFFF` opacity `0.20` | 점선 세로 경계선 4개 |
| 하프스페이스 강조 | `#FFFFFF` opacity `0.08` | 채널 2·4 면 채움 |
| 압박 라인 | `#FB923C` (orange-400) | 실선 두께 `0.5` + 우측에 `압박 라인 y=68` 라벨 |
| 콤팩트니스 | `#38BDF8` (sky-400) | 점선 사각 테두리 + `세로 32.5m × 가로 41.1m` 라벨 |
| 오버로드 `strong` (diff ≥ 2) | `#FACC15` opacity `0.35` | 구역 면 채움 |
| 오버로드 `weak` (diff = 1) | `#FACC15` opacity `0.15` | 구역 면 채움 |

**오버로드 두 단계는 반드시 같은 색상의 농도 차이로 표현한다.** 서로 다른 색(예: 노랑/주황)을 쓰면 "강도 차이"가 아니라 "다른 종류"로 읽힌다.

**색만으로 구분하지 않는다.** 오버로드 구역에는 `+2`, `+1` 숫자 라벨을 함께 표기한다. 색각 이상 사용자를 위한 규정이자, PNG로 압축됐을 때 농도 차이가 뭉개지는 것에 대한 대비이기도 하다.

압박 라인(주황 선)과 오버로드(노랑 면)는 색상환에서 가깝지만 **형태가 선 대 면으로 다르므로** 실사용에서 혼동되지 않는다. 실제 사용 중 구분이 어렵다고 판단되면 압박 라인을 `#FB7185`(rose-400)로 옮긴다.

### 12.4 UI 색 (피치 밖)

**2026-09-11 갱신 — 라이트 모드에서 다크(테크니컬 톤)로 전환.** [Hallmark](https://www.usehallmark.com/) 감사 스킬로 앱을 진단한 결과 "Tailwind 기본 팔레트를 손 안 댄 shadcn/ui 초기 상태 그대로"라는 지적(순백 배경, 폰트 페어링 없음, 표준 SaaS 내비게이션 등)을 받았고, 사용자가 직접 톤을 고른 결과다(테크니컬·데이터 중심 — Bloomberg Terminal/Linear 데이터 화면 느낌). 아래는 갱신된 값이며, 원래 라이트 모드 표는 히스토리로만 남긴다.

**원래 결정(2026-09-01, 라이트 모드)의 취지는 유지한다**: "강조색을 피치의 초록과 같은 계열로 묶는다"는 원칙은 그대로 가져가되, 어두운 배경 위에서 선명하게 보이는 톤으로 값만 조정했다 — 그래서 `--primary`는 emerald 계열을 벗어나지 않는다. 실제 CSS 커스텀 프로퍼티 값은 `frontend/src/index.css` 참조(HSL, shadcn 관례 유지).

| 용도 | 새 값(다크) | ~~예전 값(라이트, 2026-09-01~2026-09-10)~~ |
| --- | --- | --- |
| 배경 | 진한 슬레이트(`224 24% 7%`) | ~~`white`~~ |
| 카드 | 배경보다 살짝 밝음(`224 22% 10%`, 그림자 대신 명도로 입체감) | ~~`slate-50`~~ |
| 본문 텍스트 | 오프화이트(`210 20% 96%`), 보조 `215 15% 60%` | ~~`slate-900`, 보조 `slate-500`~~ |
| 강조 (선택된 국면 탭, 주요 버튼) | 터미널 그린(`152 69% 45%`) | ~~`emerald-600`~~ |
| 켜진 레이어 칩 | 어두운 그린 배경 + 밝은 그린 텍스트 | ~~`emerald-100` 배경 + `emerald-800` 텍스트~~ |
| 꺼진 레이어 칩 | 어두운 슬레이트 배경/텍스트 | ~~`slate-100` 배경 + `slate-600` 텍스트~~ |
| 비활성 (서버 미기동, 상대 좌표 없음) | 어두운 슬레이트 텍스트 + `cursor-not-allowed` | ~~`slate-300` 텍스트~~ |
| 파괴적 동작 (삭제) | 밝힌 로즈(`350 85% 58%`, 어두운 배경 대비용) | ~~`rose-600`~~ |

**폰트**(같은 감사에서 "폰트 페어링 없음, 시스템 기본값" 지적 — 함께 도입): 헤딩(h1–h3, `index.css` `@layer base`)은 **Nanum Gothic Coding**(한글까지 포함하는 진짜 monospace), 본문은 **IBM Plex Sans KR**. 처음엔 헤딩에 라틴 전용 IBM Plex Mono + 한글은 Plex Sans KR로 폴백하는 조합을 썼는데, Playwright 스크린샷으로 실제 화면을 보니 "감독 스타일로 시작하기"처럼 스페이스로 띄어 쓴 한글 헤딩에서 단어 사이 간격이 유독 벌어져 보였다 — 스페이스(U+0020) 글자 자체는 라틴 폰트인 Mono의 넓은 고정폭 그대로 남고, 앞뒤 한글 글자만 Sans KR로 폴백되면서 리듬이 어긋난 것. 한글까지 고정폭인 폰트 하나로 교체해 해결했다(폴백 경계 자체가 없어짐). `frontend/index.html`에서 Google Fonts로 불러온다.

**미확인**: iOS Safari·Android Chrome 실기기에서 다크 배경 대비·폰트 렌더링은 이 문서 갱신 시점(2026-09-11)에 AI 세션이 브라우저로 확인한 상태이며(Playwright, headless Chromium), 실기기 검증은 여전히 사람 몫이다(6단계 §10과 같은 범주).

### 12.5 공유 카드 (ShareCard)

| 요소 | 값 |
| --- | --- |
| 카드 배경 | `#0F172A` (slate-900) |
| 제목 (매치명) | `#F8FAFC`, 48px, bold |
| 부제 (일자·대회) | `#94A3B8`, 28px |
| 국면 라벨 | `#34D399`, 32px, bold |
| 본문 (코멘트/종합 평가) | `#E2E8F0`, **32px 이상**, line-height 1.5 |
| 여백 | 카드 가장자리에서 64px |

- 카드는 **어두운 배경**을 쓴다. SNS 피드에서 흰 카드보다 눈에 띄고, 피치의 진한 초록과 이어진다. 이 색은 §12.4가 다크로 바뀌기 전부터 이미 어두웠다 — 애초에 공유 카드만 먼저 다크였던 셈이라, 2026-09-11 갱신은 앱 전체를 이 카드의 톤에 맞춘 것에 더 가깝다.
- **본문 최소 32px**(1080px 카드 기준)는 타협하지 않는다. 모바일 피드에서 카드는 화면 폭에 맞춰 축소되므로, 이보다 작으면 읽을 수 없다. 텍스트가 길어 넘치면 폰트를 줄이지 말고 말줄임 처리하거나 카드를 나눈다.
- 하단 텍스트는 국면 코멘트 / 종합 평가 중 선택한다(§10).
- **폰트(2026-09-11 추가)**: 제목(매치명)은 §12.4와 같은 Nanum Gothic Coding, 나머지(부제·국면 라벨·본문)는 IBM Plex Sans KR — 원래 `system-ui, sans-serif`였던 것을 앱 전역 폰트 페어링에 맞췄다.

## 13. 요구사항 ↔ 설계 매핑

| FR | 요구사항 | 설계 절 |
| --- | --- | --- |
| FR-01 | 피치·선수 배치, 드래그 편집 | §3, §7, §11.4, §12.2 |
| FR-02 | 국면 모핑 애니메이션 | §8, §11.1(탭 배치), §12.2(Ghost) |
| FR-03 | 공간 시각화 레이어 | §3.1, §9, §11.1(토글 배치), §12.3 |
| FR-04 | 국면별 텍스트 입력 | §7, §11.1 |
| FR-05 | PNG 카드 내보내기 | §10, §12.5 |
| FR-06 | 포메이션 프리셋 작성 흐름 | §2(`formations.ts`), §6(`/new`), §11.3 |
| FR-07 | JSON 내보내기/가져오기 | §5(직렬화 공유), §7 |
| FR-08 | DB 저장·목록 조회 | §1(선택적 의존성), §4, §5, §6, §11.3 |

## 14. 다음 단계
`3단계_기술_규격_및_API.md`에서 TypeScript 타입, API 요청/응답 상세 규격, 패키지 목록, 환경 설정, 실행 스크립트를 확정한다.
